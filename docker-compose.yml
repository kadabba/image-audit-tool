version: '3.8'

services:
  app:
    build: ./backend
    container_name: image-audit-tool
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
